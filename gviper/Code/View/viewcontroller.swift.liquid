import UIKit

// MARK: - {{ module_info.name }}ViewController implementation 

final class {{ module_info.name }}ViewController: UIViewController {

    // MARK: Properties

    private let presenter: {{ module_info.name }}ViewOutput

    // MARK: Life cycle

    init(presenter: {{ module_info.name }}ViewOutput) {
        self.presenter = presenter
        super.init(nibName: nil, bundle: nil)
    }

    required init?(coder: NSCoder) {
        fatalError("init(coder:) has not been implemented")
    }

    override func viewDidLoad() {
        super.viewDidLoad()
        presenter.viewIsReady()
    }

}

// MARK: - {{module_info.name}}ViewInput implementation

extension {{module_info.name}}ViewController: {{module_info.name}}ViewInput {
    func setupInitialState() {
    }
}
